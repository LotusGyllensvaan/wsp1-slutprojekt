<h1>Order Confirmation</h1>

<p><strong>Order ID:</strong> <%= order['id'] %></p>
<p><strong>Total:</strong> $<%= sprintf('%.2f', order['total']) %></p>
<p><strong>Payment Status:</strong> <%= payment['status'] %></p>
<p><strong>Payment Provider:</strong> <%= payment['provider'] %></p>

<h2>Items in Your Order:</h2>
<ul>
  <% purchases.each do |purchase| %>
    <li>
      <strong><%= purchase['name'] %></strong> - $<%= sprintf('%.2f', purchase['price']) %> x <%= purchase['quantity'] %>
      (Added on <%= purchase['created_at'] %>)
      <p>$<%= sprintf('%.2f', purchase['price'].to_f * purchase['quantity'].to_f) %></p>
    </li>
  <% end %>
</ul>

<a href="/">Return to Home</a>
